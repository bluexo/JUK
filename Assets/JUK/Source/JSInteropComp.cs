using System.Collections;
using System.Collections.Generic;
using Jint;
using Jint.Native;
using UnityEngine;

namespace JUK
{
    public class JSInteropComp : MonoBehaviour
    {
        public JsValue This { get; private set; }

        private void Awake()
        {
        }

        // Start is called before the first frame update
        private void Start()
        {

        }

        // Update is called once per frame
        private void Update()
        {

        }
    }
}
